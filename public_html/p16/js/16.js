
var base = parseInt(prompt('Type the base'));
var height = parseInt(prompt('Type the height'));

perimeter = base + base + height + height;

   console.log("Perimeter is  = " + perimeter);
   
area = base * height;

   console.log("Area is = " + area);